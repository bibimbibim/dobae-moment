chrome.runtime.onInstalled.addListener(() => {
    console.log('[background] chrome.runtime.onInstalled initialize');
    console.log('[background] chrome.runtime.onInstalled initialize');
});

chrome +